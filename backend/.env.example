# --- Admin login (used by the admin UI) ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
JWT_SECRET=please-change-this-to-a-long-random-string

# --- Chain access (read-only queries) ---
# A Sepolia JSON-RPC endpoint (Infura/Alchemy/public).
RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
CHAIN_ID=11155111

# Live composite-DID registry that decides voter eligibility (read-only).
# Defaults to the deployed one; override only if you point at a different registry.
DID_REGISTRY=0xb1768B404EB4102CCF4DBc0c9b661a17D48dcef8

# Server
PORT=4000
